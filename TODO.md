# TODO

- [ ] Заменить `sys.path` хаки в `backend/main.py` на нормальные импорты:
      `from brain_morph.utils import Volume, MeshTransformer3D`
      `from brain_morph.registration import RegistrationPipeline, Stage, ...`
